// require('dotenv').config();
// const { pickPhoto } = require('./pick');

// const { gotYouEvent, pickEvent } = require('../lib/eventWorker');

// jest.mock('pickPhoto');

// describe('Test eventWorker', () => {
//   test('Can pick random photo', async () => {
//     pickPhoto.mockResolvedValue({ 'event': { reply:  } });
//   });
// });
